# 🌐 Создание APK через Expo веб-интерфейс

## 🎯 Метод 1: Через EAS Build (Самый простой)

### Шаг 1: Подготовка
```powershell
# В терминале выполните:
eas login
```

Введите email и пароль от expo.dev (или создайте новый аккаунт прямо здесь)

### Шаг 2: Инициализация проекта
```powershell
eas init
```

Если спросит - подтвердите создание нового проекта.

### Шаг 3: Запуск сборки
```powershell
eas build --platform android --profile preview --non-interactive
```

### Шаг 4: Мониторинг сборки
После запуска команды вы получите ссылку типа:
```
https://expo.dev/accounts/YOUR_ACCOUNT/projects/rollshousepizza/builds/BUILD_ID
```

Откройте эту ссылку в браузере и наблюдайте за процессом!

---

## 🎯 Метод 2: Полностью через веб-интерфейс

### 1. Регистрация на Expo
- Перейдите: https://expo.dev
- Нажмите "Sign Up"
- Зарегистрируйтесь через Email или GitHub

### 2. Создание проекта
- Нажмите "Create a new project"
- Название: `rollshousepizza`
- Выберите: "Blank workflow"

### 3. Подключение кода

#### Вариант A: Через Git
```powershell
# 1. Инициализируйте Git (если еще не сделали)
git init
git add .
git commit -m "Initial commit"

# 2. Создайте репозиторий на GitHub

# 3. Свяжите локальный проект с GitHub
git remote add origin https://github.com/YOUR_USERNAME/rollshousepizza.git
git branch -M main
git push -u origin main

# 4. В веб-интерфейсе Expo подключите GitHub репозиторий
```

#### Вариант B: Через командную строку (проще)
```powershell
# Просто выполните команды из Метода 1
eas login
eas build --platform android --profile preview
```

### 4. Запуск сборки через веб
1. Откройте https://expo.dev
2. Выберите ваш проект `rollshousepizza`
3. Перейдите в "Builds"
4. Нажмите "Create a build"
5. Выберите:
   - Platform: **Android**
   - Build type: **APK** (не AAB)
   - Profile: **preview**
6. Нажмите "Build"

### 5. Скачивание APK
После завершения (5-15 минут):
1. В списке сборок появится ваша сборка
2. Статус изменится на "Finished"
3. Нажмите на сборку
4. Нажмите кнопку "Download"
5. APK файл скачается на ваш компьютер

---

## 🎯 Метод 3: Expo Snack (Для тестирования)

Если нужно только протестировать приложение:

1. Перейдите: https://snack.expo.dev
2. Скопируйте туда ваш код
3. Установите Expo Go на телефон
4. Отсканируйте QR код

⚠️ **Примечание**: Snack НЕ создает APK, только для тестирования!

---

## 📱 Метод 4: APK из Expo Go (Временное решение)

Пока создается настоящий APK, можете использовать:

1. Установите **Expo Go** на Android:
   - Google Play: https://play.google.com/store/apps/details?id=host.exp.exponent
   
2. В терминале запустите:
```powershell
npm run start
```

3. Отсканируйте QR код через Expo Go

⚠️ **Это НЕ APK**, но позволяет тестировать приложение!

---

## 🔧 Устранение проблем

### Ошибка "403 Forbidden"
**Причина**: Не выполнен вход в аккаунт

**Решение**:
```powershell
eas logout
eas login
```

### Ошибка "Invalid UUID appId"
**Причина**: Некорректный projectId в app.json

**Решение**: Удалите секцию `extra.eas.projectId` из app.json и выполните:
```powershell
eas init
```

### Ошибка "Project not found"
**Причина**: Проект не связан с вашим аккаунтом Expo

**Решение**:
```powershell
eas init --force
```

---

## 📊 Сравнение методов

| Метод | Сложность | Время | Требования |
|-------|-----------|-------|------------|
| **EAS Build (CLI)** | ⭐⭐ Легко | 5-15 мин | Аккаунт Expo |
| **Веб-интерфейс** | ⭐⭐⭐ Средне | 5-15 мин | Аккаунт + Git |
| **Локальная сборка** | ⭐⭐⭐⭐⭐ Сложно | 30+ мин | Android Studio |
| **Expo Snack** | ⭐ Очень легко | 1 мин | Только тест |

---

## 💡 Рекомендация

**Используйте Метод 1 (EAS Build через CLI)** - это самый простой способ!

Полная команда для копирования:
```powershell
# 1. Войти в аккаунт
eas login

# 2. Создать APK
eas build --platform android --profile preview

# 3. Дождаться завершения и скачать по ссылке
```

---

## 📞 Поддержка

- Документация: https://docs.expo.dev
- Discord: https://discord.gg/expo
- Форум: https://forums.expo.dev

