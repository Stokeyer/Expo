# 🚀 Загрузка проекта на GitHub

## 📋 Предварительные требования

1. **Git установлен** на компьютере
   - Проверка: `git --version`
   - Скачать: https://git-scm.com/download/win

2. **Аккаунт GitHub**
   - Регистрация: https://github.com/signup

---

## 🎯 Способ 1: Через командную строку (Рекомендуется)

### Шаг 1: Инициализация Git в проекте

Откройте терминал в папке проекта и выполните:

```powershell
# Перейдите в папку проекта
cd my-expo-app

# Инициализируйте Git
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: Rolls House Pizza App"
```

### Шаг 2: Создайте репозиторий на GitHub

1. Откройте https://github.com
2. Нажмите **"+"** → **"New repository"**
3. Заполните:
   - **Repository name**: `rollshousepizza`
   - **Description**: `Приложение для доставки японской еды`
   - **Public** или **Private** (на ваш выбор)
   - ❌ НЕ ставьте галочки на README, .gitignore, license
4. Нажмите **"Create repository"**

### Шаг 3: Свяжите локальный проект с GitHub

GitHub покажет команды. Выполните в терминале:

```powershell
# Замените YOUR_USERNAME на ваше имя пользователя GitHub
git remote add origin https://github.com/YOUR_USERNAME/rollshousepizza.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Загрузите код на GitHub
git push -u origin main
```

### Шаг 4: Введите учетные данные

При первой загрузке GitHub попросит:
- **Username**: ваш логин GitHub
- **Password**: используйте **Personal Access Token** (не обычный пароль!)

#### Как создать Personal Access Token:
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token → Generate new token (classic)
3. Выберите срок действия и отметьте `repo`
4. Скопируйте токен (он больше не покажется!)
5. Используйте его вместо пароля

---

## 🎯 Способ 2: Через GitHub Desktop (Проще для новичков)

### Шаг 1: Установите GitHub Desktop
Скачайте: https://desktop.github.com/

### Шаг 2: Добавьте проект
1. Откройте GitHub Desktop
2. File → Add Local Repository
3. Выберите папку `my-expo-app`
4. Если Git не инициализирован, нажмите **"Create a repository"**

### Шаг 3: Создайте коммит
1. В левой панели увидите все файлы
2. Внизу введите: "Initial commit"
3. Нажмите **"Commit to main"**

### Шаг 4: Опубликуйте на GitHub
1. Нажмите **"Publish repository"**
2. Заполните название: `rollshousepizza`
3. Выберите Public/Private
4. Нажмите **"Publish Repository"**

---

## 🎯 Способ 3: Через VS Code (Если используете VS Code)

### Шаг 1: Инициализация
1. Откройте проект в VS Code
2. Нажмите на иконку **Source Control** (слева)
3. Нажмите **"Initialize Repository"**

### Шаг 2: Создайте коммит
1. В Source Control нажмите **"+"** возле всех файлов (Stage All Changes)
2. Введите сообщение: "Initial commit"
3. Нажмите **"✓ Commit"**

### Шаг 3: Загрузите на GitHub
1. Нажмите **"..."** → **"Push to..."** → **"GitHub"**
2. Войдите в GitHub
3. Выберите имя репозитория: `rollshousepizza`
4. Выберите Public/Private

---

## 📝 Полезные команды Git

### Обновление кода на GitHub после изменений:
```powershell
# Добавить все изменения
git add .

# Создать коммит с описанием
git commit -m "Описание изменений"

# Загрузить на GitHub
git push
```

### Проверка статуса:
```powershell
# Посмотреть какие файлы изменены
git status

# Посмотреть историю коммитов
git log --oneline
```

### Клонирование на другой компьютер:
```powershell
git clone https://github.com/YOUR_USERNAME/rollshousepizza.git
```

---

## 🔐 Настройка SSH (Рекомендуется для удобства)

Чтобы не вводить токен каждый раз:

### Шаг 1: Создайте SSH ключ
```powershell
ssh-keygen -t ed25519 -C "your_email@example.com"
```

Нажимайте Enter, Enter, Enter (можно оставить пустым)

### Шаг 2: Скопируйте публичный ключ
```powershell
# Windows
type ~\.ssh\id_ed25519.pub

# Скопируйте весь вывод
```

### Шаг 3: Добавьте ключ на GitHub
1. GitHub → Settings → SSH and GPG keys → New SSH key
2. Вставьте скопированный ключ
3. Нажмите Add SSH key

### Шаг 4: Измените URL репозитория
```powershell
git remote set-url origin git@github.com:YOUR_USERNAME/rollshousepizza.git
```

Теперь можно делать `git push` без ввода пароля!

---

## 📦 Что будет загружено на GitHub

✅ Исходный код всех компонентов
✅ Конфигурационные файлы (app.json, eas.json, package.json)
✅ Файлы стилей
✅ Ассеты (изображения, иконки)
✅ Документация

❌ Не будет загружено (благодаря .gitignore):
- node_modules/
- .expo/
- Временные файлы
- Локальные настройки

---

## 🎨 Улучшение README

После загрузки создайте файл README.md в корне проекта:

```markdown
# 🍱 Rolls House Pizza

Приложение для доставки японской еды на React Native с Expo.

## 🚀 Возможности
- Просмотр меню
- Добавление товаров в корзину
- Красивые анимации
- Адаптивный дизайн

## 📱 Установка
\`\`\`bash
npm install
npm start
\`\`\`

## 🔧 Создание APK
\`\`\`bash
eas build --platform android --profile preview
\`\`\`

## 🛠 Технологии
- React Native
- Expo
- TypeScript
- NativeWind (Tailwind CSS)
- React Native Animated
```

---

## 🔗 После загрузки на GitHub

### Подключите к Expo для автоматической сборки:

1. Откройте https://expo.dev
2. Войдите в аккаунт
3. Create new project → Import from GitHub
4. Выберите репозиторий `rollshousepizza`
5. Теперь можете запускать сборки через веб-интерфейс!

---

## ❓ Устранение проблем

### Ошибка "Repository not found"
```powershell
# Проверьте URL
git remote -v

# Исправьте если нужно
git remote set-url origin https://github.com/YOUR_USERNAME/rollshousepizza.git
```

### Ошибка "Permission denied"
- Используйте Personal Access Token вместо пароля
- Или настройте SSH (см. выше)

### Ошибка "Updates were rejected"
```powershell
# Сначала получите изменения с GitHub
git pull origin main

# Затем загрузите свои
git push origin main
```

---

## 📊 Быстрая шпаргалка

```powershell
# Первая загрузка
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/USERNAME/REPO.git
git push -u origin main

# Последующие обновления
git add .
git commit -m "Описание изменений"
git push
```

---

## 💡 Совет

После загрузки на GitHub, ваш проект будет доступен по адресу:
```
https://github.com/YOUR_USERNAME/rollshousepizza
```

Можете поделиться этой ссылкой с другими! 🎉

