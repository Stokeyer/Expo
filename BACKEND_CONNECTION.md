# Решение проблем подключения к бэкенду

## Проблема: "Бэкенд не виден" или "Network request failed"

### Быстрая диагностика:

1. **Проверьте, что бэкенд запущен:**
   ```bash
   cd backend
   npm run start:dev
   ```
   
   Должно появиться:
   ```
   🚀 Server is running on http://localhost:3000
   ```

2. **Проверьте в браузере:**
   Откройте: `http://localhost:3000/api/orders/test`
   
   Должен вернуться JSON с сообщением.

3. **Проверьте логи в Expo:**
   При запуске приложения должны появиться логи:
   ```
   🌐 API Configuration:
      Mode: development
      Platform: ios (или android)
      API URL: http://localhost:3000
      Orders endpoint: http://localhost:3000/api/orders
   ```

### Решения в зависимости от платформы:

#### Для iOS симулятора:
- ✅ `http://localhost:3000` должен работать
- Если не работает, проверьте, что бэкенд запущен

#### Для Android эмулятора:
- ❌ `http://localhost:3000` НЕ работает
- ✅ Используйте `http://10.0.2.2:3000` вместо localhost
- Измените в `Expo/config/api.ts`:
  ```typescript
  android: 'http://10.0.2.2:3000',
  ```

#### Для физического устройства (iOS/Android):
- ❌ `http://localhost:3000` НЕ работает
- ✅ Используйте IP-адрес вашего компьютера
- Узнайте IP-адрес:
  - Windows: `ipconfig` (ищите IPv4 адрес)
  - Mac/Linux: `ifconfig` или `ip addr`
- Измените в `Expo/config/api.ts`:
  ```typescript
  android: 'http://ВАШ_IP_АДРЕС:3000',
  ios: 'http://ВАШ_IP_АДРЕС:3000',
  ```
- Убедитесь, что устройство и компьютер в одной Wi-Fi сети
- Проверьте, что файрвол не блокирует порт 3000

#### Для веб-браузера:
- ✅ `http://localhost:3000` должен работать
- Если не работает, проверьте, что бэкенд запущен

### Автоматическая проверка:

Приложение теперь автоматически проверяет доступность бэкенда перед отправкой заказа. Если бэкенд недоступен, вы увидите сообщение об ошибке.

### Проверка вручную через curl:

```bash
# Проверка тестового endpoint
curl http://localhost:3000/api/orders/test

# Проверка создания заказа
curl -X POST http://localhost:3000/api/orders \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "+7 (999) 123-45-67",
    "address": "ул. Тестовая, д. 1",
    "items": [{"id": 1, "name": "Пицца", "quantity": 1, "price": 500}],
    "totalPrice": 500
  }'
```

### Частые ошибки:

1. **"Network request failed"**
   - Бэкенд не запущен
   - Неправильный URL (используйте IP для физических устройств)
   - Файрвол блокирует порт 3000

2. **"CORS error"**
   - CORS уже настроен в бэкенде
   - Перезапустите бэкенд после изменений

3. **"Connection refused"**
   - Бэкенд не запущен
   - Неправильный порт (должен быть 3000)


