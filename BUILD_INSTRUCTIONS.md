# 📱 Инструкция по созданию APK файла

## 🚀 Способ 1: EAS Build (Рекомендуется)

### Шаг 1: Установка EAS CLI
```bash
npm install -g eas-cli
```

### Шаг 2: Вход в аккаунт Expo
```bash
eas login
```
Если у вас нет аккаунта, создайте его на https://expo.dev

### Шаг 3: Настройка проекта
```bash
cd my-expo-app
eas build:configure
```

### Шаг 4: Создание APK
Для тестовой версии (быстрее):
```bash
eas build --platform android --profile preview
```

Для production версии:
```bash
eas build --platform android --profile production
```

### Шаг 5: Скачивание APK
После завершения сборки, EAS предоставит ссылку для скачивания APK файла.
Вы можете скачать его прямо из браузера или через:
```bash
eas build:list
```

---

## 🔧 Способ 2: Локальная сборка (Требует Android Studio)

### Требования:
- Android Studio
- Java Development Kit (JDK)
- Android SDK

### Шаг 1: Prebuild
```bash
cd my-expo-app
npx expo prebuild --platform android
```

### Шаг 2: Сборка APK
```bash
cd android
./gradlew assembleRelease
```

APK будет находиться в:
```
android/app/build/outputs/apk/release/app-release.apk
```

---

## 📦 Способ 3: Expo Build (Старый способ - Deprecated)

⚠️ Этот способ устаревший, но все еще работает:

```bash
cd my-expo-app
expo build:android -t apk
```

---

## 🎯 Рекомендация

**Используйте Способ 1 (EAS Build)** - это самый простой и современный способ:
1. Не требует установки Android Studio
2. Сборка происходит на серверах Expo
3. Автоматическая подпись приложения
4. Простое управление версиями

### Пример полного процесса:
```bash
# 1. Установка EAS CLI (один раз)
npm install -g eas-cli

# 2. Вход в аккаунт
eas login

# 3. Переход в папку проекта
cd my-expo-app

# 4. Настройка (первый раз)
eas build:configure

# 5. Создание APK
eas build --platform android --profile preview

# 6. Ожидание (5-15 минут)
# 7. Скачивание APK по ссылке
```

---

## 📝 Полезные команды

Просмотр всех сборок:
```bash
eas build:list
```

Отмена текущей сборки:
```bash
eas build:cancel
```

Проверка статуса:
```bash
eas build:view
```

---

## 🔐 Подпись APK

EAS автоматически создает и управляет ключами для подписи.
Для production сборки ключи сохраняются в вашем аккаунте Expo.

---

## 💡 Советы

1. **Первая сборка**: Может занять 10-20 минут
2. **Последующие сборки**: 5-10 минут
3. **Размер APK**: Примерно 40-60 MB
4. **Бесплатный план**: 30 сборок в месяц

---

## 🐛 Устранение проблем

### Ошибка: "No valid Android credentials"
```bash
eas credentials
```

### Ошибка при установке APK
- Включите "Установка из неизвестных источников" на Android устройстве
- Путь: Настройки → Безопасность → Неизвестные источники

### APK слишком большой
Добавьте в `app.json`:
```json
"android": {
  "enableProguardInReleaseBuilds": true,
  "enableShrinkResourcesInReleaseBuilds": true
}
```

